import {GameFacade} from '../domain/game-facade';

// const currentClient = new WebClient();
// const opponentClient = getOpponentClient();
//
// (async () => {
//   await GameFacade.startGame(currentClient, opponentClient);
// })();
